apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-1
  zones: ["us-east-1a", "us-east-1b"]

autoModeConfig:
  enabled: false

managedNodeGroups:
  - name: roboshop-nodes
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 2